# 流程优化 review

> 对象：已实现的**回填 / 日增量 / 压实 / 复权 / 指标物化**流程。
> 方法：真实数据实测，不靠推断。

---

## 结论：6 个问题，4 个已修

| # | 问题 | 实测证据 | 状态 |
|---|---|---|---|
| 1 | **入库后不自动压实** | 全表 count 0.03 s → **2.52 s（84×）**；复权因子构建从**能跑变成 OOM** | ✅ 已修 |
| 2 | **压实粒度是整表** | 整表 17.5 s，而日增量只改当年分区 | ✅ 已修（分区级） |
| 3 | **日增量流程不存在** | `dump_daily_k_10d` 声明了但**零引用** | ✅ 已修（`incremental()`） |
| 4 | **无编排命令** | 日流程 4~5 个手动步骤，顺序错就出错 | ✅ 已修（`xlp daily`） |
| 5 | 复权因子每次重写 **1,029 万行** | 33 s/次 | ⏳ 见 §4 |
| 6 | 下载缓存**从不清理** | **173 MB**（2C2G 上占近一半磁盘） | ⏳ 见 §5 |

---

## 1. 入库后不自动压实（最严重）

追加写入会作废压实标记，读取退回带 `row_number()` 去重窗口的慢路径：

| 查询 | 已压实 | 未压实 | 倍数 |
|---|---|---|---|
| 全表 `count(*)` | **0.03 s** | 2.52 s | **84×** |
| 全表 `DISTINCT thscode` | **0.09 s** | 2.94 s | **33×** |
| 10 年全表窗口计算 | **7.87 s** | 15.35 s | 2× |

**更严重的**：复权因子构建（DuckDB 限额 700 MB）

| 状态 | 结果 |
|---|---|
| 已压实 | ✅ 成功 33 s |
| **未压实** | **❌ OOM 崩溃** |

原因是未压实时，去重窗口与因子计算的窗口**叠加**，峰值超出限额。

> 修法：`backfill()` / `incremental()` 默认入库后自动压实，
> 并提供 `--no-compact` 逃生口。压实步骤**不计入 `total_rows`**
> （它是重写既有数据，不增加行数）。

## 2. 压实粒度：整表 → 分区

日增量只改动**当年分区**，但压实原本重写全部 11 个分区（17.5 s）。

修法：`Compactor.compact(table, partitions=[...])`，只压受影响分区。
语义上必须注意：**指定分区时不能因「整表已压实」而跳过** ——
该分区可能正是新写入的那个。

## 3. 日增量流程（`incremental()`）

```
dump_daily_k_10d → 下载 1.02 MB → 入库 → 压实受影响分区
```

为什么用 10 日增量而不是「逐只拉当天」：

| 方式 | 请求数 | 耗时 |
|---|---|---|
| `dump_daily_k_10d` | **1** | **1.1 s** |
| `prices_historical` 逐只 | 5,573 | ≈ 20 分钟 |

且它自带 **10 交易日重叠窗口** —— 漏跑几天自动补，重复跑靠主键去重。

## 4. 待办：复权因子每次重写 1,029 万行

现状 `AdjustFactorBuilder.build()` 每次全量重算（33 s / 1,029 万行 / 2.5 MB）。

`forward_factor = C_last / C_t` —— 新事件会改变 `C_last`，**所有行确实都变**，
所以全量重写看似必要。但：

- **`cum_scale = C_t` 只在除权日变化，历史值永不回溯改变**
- 把 `cum_scale` 烘焙进压实后的 kline（写时算一次），`C_last` 用 5,564 行小表

收益：日增量不再重写 1,029 万行；`v_daily_qfq` 从「1,029万 × 1,029万 JOIN」
变成「1,029万 × 5,564 JOIN」。
代价：新事件到达时需要重刷受影响分区。

## 5. 待办：下载缓存无清理

`downloads/` 累计 173 MB（10 年全量 dump 181 MB 常驻），**从不清理**。
在 2C2G 目标机上这是近一半磁盘（数据本身才 180 MB）。

建议：全量 dump 成功入库后即删（可随时重新下载）；
增量 dump 保留最近 N 天；`xlp doctor` 提示缓存体积。

## 6.（记录）一个**看着很美但是错**的优化

想把「`events ASOF JOIN kline`（5.7 万 × 1,029 万）」换成
「先用全局日历求前一交易日，再等值 JOIN」：

| 写法 | 耗时 | 峰值内存增量 |
|---|---|---|
| 现状（逐股 ASOF） | 3.3 s | +803 MB |
| 「优化」（全局日历） | **0.5 s** | **+0 MB** |

**但结果差 399 行，全是丢行**：

```
000901.SZ ex_date=2020-03-06
  正确 prev_date = 2020-02-26（相隔 9 天 —— 该股中途停牌）
  错误 prev_date = 全局前一交易日，该股当日无行情 → 整条事件被丢弃
```

**逐股 ASOF 是语义必需的**：它取的是「**该股自己**的上一根 K 线」，
天然处理停牌。全局日历做不到。

> 记下来防止以后（包括我自己）再"优化"一次。
> 这类优化必须先做**逐值差异检查**，只看耗时和内存会直接踩进去。

---

## 附：优化后的 `xlp daily`

```
xlp daily                 # 增量入库 → 压实 → 重建复权因子 → 物化指标 → 校验
xlp daily --skip-incremental
xlp daily --date 2026-09-16
```

顺序**不可交换**：复权因子依赖压实的 K 线，指标依赖复权因子。
任一步失败即中断（`--skip-*` 仅用于调试）。
