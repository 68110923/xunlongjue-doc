<!-- ⚠️ 本文件由私有源码仓库自动同步，请勿直接在本仓库修改。 -->
<!-- 源文件：README.md（与 docs/site/index.html 同源，CI 有守卫）。 -->

# 寻龙诀 PRO · A 股一进二竞价打板量化系统

> **一次买断、永久使用**：源码交付 + 部署教程 + 十年全市场数据湖 + 每个交易日自动选股与复盘。
> PRO 版定价 **1288 元**（原版 xunlongjue 888 元，见「PRO 版新增」一节）。
>
> ⚠️ 仅供**研究与学习**，**不构成任何投资建议**，**不承诺任何收益**。

线上宣传页与本文件**同源**（页面直接从本文件读取通知与更新日志，CI 有守卫）。

---

## 📢 通知

### PRO 版上线：全市场竞价 + 51 条质量闸 `20260923-20261231`
- **竞价升级**：从「只看昨日涨停池」扩成**全市场每日每票一行**（含未匹配量、竞价换手、竞价量比、占昨日成交量比）。
  选票用的池票**必须 100% 定盘**才发布结果 —— 竞价没定盘就不出产物，宁可不发也不拿虚拟参考价凑数。
- **工程质量**：**51 条质量断言 + 3 条复权不变量**每天盘后自动跑，不过即退出码非零；16 个作业一键安装，法定休市日自动跳过。
- **价格**：PRO 版源码 **1288 元**（原版 xunlongjue 888 元）；**试用 88 元可全额抵扣**（1288 → 1200）。

### 早盘出结果时间提前到 09:25:20 起 `20260923-20261231`
竞价窗口从 09:26 挪到 **09:25:20**（实测 09:25:13 采到的是虚拟参考价），
一次取全市场 ≈56 个请求、并发后约 5 秒，**竞价落盘即选票**，09:25:30 前结果就位。

---

## 🎯 这是什么

一套**部署在自己电脑或服务器上**的 A 股量化研究工具。它在本地维护一份覆盖**十年全市场**的数据湖，
每个交易日自动完成：盘前竞价采集 → 一进二选票 → 盘中强多扫描 → 盘后复盘与明日观察，
全部结果结构化落表。

结果有两个出口：**网页看板**随时查看，或**推送到微信**（含 AI 分析）。两者都从同一份数据取数、
**互不依赖**；核心层**不依赖 AI**，没有 AI 也照常运行。

不连接券商账户、不代下单、不经手资金 —— 它只做「整理数据 + 给出研究结论」。

```
                    ┌─→ 网页看板（只读表，零 LLM）
xlp 核心层（写表）──┤
                    └─→ 微信推送 / 对话（AI 增强层，可选）
```

## 🆚 PRO 版新增（对比原版）

| 维度 | 原版 xunlongjue（888 元） | **PRO 版（1288 元）** |
|---|---|---|
| 数据源 | 自建 SQLite 库 + 第三方接口拼数据 | **同花顺 Fuyao API**，31 个端点全部实测（批量上限/延迟/字段可得性写进文档） |
| 竞价数据 | 池内票竞价，且**88.9% 的行用全天成交量冒充竞价量** | **全市场每日每票一行**的**真实**竞价：未匹配量、竞价换手率、竞价量比、占昨日量比 |
| 涨跌停判定 | 靠名字猜板块/ST 幅度 | **权威涨停比例 + 涨停价**随行落库（`limit_ratio` / `limit_up_price`），「一字」判定不再猜 |
| 数据组织 | 一张大表 + 手写 SQL | **本地数据湖**：17 张表按主键只追加不覆盖、批次血缘、`v_*` 只读视图契约 |
| 质量保障 | 无自动化体检 | **51 条质量断言 + 3 条复权不变量**，每个交易日自动跑；复权口径三种锚定全部校验 |
| 调度 | 手写 crontab，休市日照跑 | **16 个作业单一真源**，一键装 systemd / crontab；**法定休市日自动跳过**并自证 |
| 机器要求 | 较宽松（不明确） | **实测 2 核 2 GB 可跑**：常驻约 325 MB，最重作业峰值约 1.1 GB |
| 取数速度 | 串行等网络 | 批量作业**并发取数**（线程安全令牌桶，默认 12 QPS / 4 线程，可调） |
| 策略强度 | 提示词 + 少量脚本 | 16 道硬闸 + 10 维评分 + 板块共振 + **期望收益**口径；参数重标定有回测 |
| 文档 | 一份 README | **18 份活文档**（策略/指标/接口/数据字典/性能/运维/部署/工作流），CI 守卫不许漂 |
| AI 层 | 强绑定 Hermes | **核心层零 LLM**；AI 层可选，不装也能跑完整流程 |

> 一句话：**同样的思路，换成了真数据、真口径、真体检**。贵 400 元买的是「数字能不能信」。

## 🧩 功能

| 功能 | 说明 |
|---|---|
| **一进二寻龙** | 全市场首板扫描，十余道硬闸 + 多维评分，每天 **9:25 竞价定盘后**给出候选（≤3 只）。每只票附「买点类型 + 触发条件 + 放弃条件」；判据含封板时间、封板资金、连板数、板块共振、竞价涨幅、竞价额、**竞价未匹配量**、竞价量比 |
| **全市场选好票** | 按技术面多维打分筛选强多票（均线 / KDJ / MACD / 布林带），盘中每 30 分钟重算，留存在榜与掉出轨迹（入选时间、掉出时间、掉出原因） |
| **每日自动复盘** | 收盘后汇总当日结果：**✅ 命中 / ⚠️ 遗漏 / 🔴 盲区**，另附一份「不该漏」清单 —— 漏在哪里、为什么漏，写清楚 |
| **明日观察** | 盘后结合涨停梯队、板块合力、封板质量与竞价抢筹，梳理次日关注方向，并对「虚高开盘」一票否决 |
| **个股 / 大盘预测** | 发代码即回方向参考与关键指标；也能看当日涨跌家数、情绪档位与大盘系数 |
| **深度分析** | 拉取长周期价量序列做结构化解析（形态、量能、指标分位），辅助判断主力行为痕迹 |
| **两个出口** | 网页看板（只读产物、自动刷新）/ 微信推送（含 AI 分析）；**任选其一或都要**，互不依赖 |

## 📊 数据能力

| 数据 | 说明 |
|---|---|
| **十年全市场日线** | **2016 → 今**，全市场五千余只标的，未复权原始数据 + 完整复权因子事件流（含配股） |
| **真实竞价明细** | 竞价成交量额、**未匹配量**、竞价换手率、竞价量比、占昨日成交量比；**全市场每日每票一行** |
| **涨停与梯队** | 涨停池、炸板池、跌停池、**连板梯队矩阵**，含权威连板数与涨停原因 |
| **资金与热度** | 龙虎榜（机构 / 游资净买卖）、热股榜与排名趋势、个股异动原因 |
| **板块体系** | 同花顺行业与概念指数成分股，可做行业级与概念级共振分析 |
| **指数与估值** | 宽基 / 行业 / 概念指数日线与指标（523 个），以及市盈率 / 市净率 / 市销率快照 |

> 全部落在**本地数据湖**：**17 张表**按主键只追加不覆盖，每个批次带血缘（批次号 + 入库时间），
> 原始层永不修改、派生层随时可重建。消费方（看板 / 脚本 / AI）**只读视图 `v_*`**，底层换实现上层无感。

## 🛡️ 工程与可靠性

| 能力 | 说明 |
|---|---|
| **一条命令跑完盘后** | `xlp daily`：增量入库 → 复权口径 → 物化指标 → 池与指数 → 质量闸，失败即退出码非零 |
| **可验证，不靠感觉** | **51 条质量断言** + 3 条复权不变量（前复权末根=真实价、后复权首根=真实价、因子恒为正），每个交易日自动跑 |
| **小机器跑得动** | 实测 **2 核 2 GB**：常驻约 325 MB，最重作业峰值约 1.1 GB；全市场竞价 56 个请求并发后约 5 秒 |
| **数据可追可回滚** | 同一主键重复写入会**报错**而不是静默覆盖；修复后按分区压实去重，读取永远取最新版本 |
| **备份与演练** | 只备份**不可再生**的原始层与状态库（约 193 MB / 2.3 秒），逐文件校验和，恢复前可先做一次不落地的演练 |
| **定时任务单一真源** | **16 个作业**的命令与计划由代码声明，一键安装 systemd / crontab；法定休市日自动跳过，漏更新日历会**拒绝执行**而不是白跑 |
| **业绩自己算** | 本页**不写胜率/收益数字**：战绩由 `xlp stats` 按你库里的真实自评**现算**，并附幸存者偏差标注 |

## 💰 定价与购买

| 项目 | 价格 | 说明 |
|---|---|---|
| **PRO 版源码** | **1288 元** | 交付源码 + 部署教程 + 全部文档；**一次买断、永久使用**；走私有仓库的可持续 `git pull` 拿更新 |
| 协助部署（可选） | 200 元 | 一对一远程，约 2 小时；不想折腾服务器就选它 |
| **试用** | **88 元** | 试用 5~7 个交易日（**不含源码**，看实际推送结果）；**试用期内购买全额抵扣**（1288 → 1200） |
| 原版 xunlongjue | 888 元 | 旧版本，仍可用；差别见「PRO 版新增」一节 |
| 云服务器（可选，自购） | 约 38~99 元/年 | **2 核 2 GB** 即可；装自己电脑上也行 |

**试用规则**：每个用户限试用一次；试用期内推送命中不足 2 只涨停，**试用期自动延长，抓到为止**；
试用期结束不再抵扣。

> 💡 想先试再买：扫码付 88 元 → 留下联系方式 → 拉你进试用群，每天在群里看推送结果。

## 🚀 怎么买（进群流程）

1. **付款**：支付宝 / 微信扫码（二维码见**宣传页**），备注你的联系方式
2. **加我**：按宣传页上的联系方式加我（也可以先聊清楚再决定）
3. **进群**：拉你进试用群（试用）或交付群（购买），群里每天同步推送结果
4. **交付**：付款后把源码发给你（私有仓库协作者 / 压缩包二选一）
5. **部署**：照 `docs/DEPLOY.md` 走，约 14 分钟灌完十年数据；也可以花 200 元让我远程搞定

| 微信收款码 | 支付宝收款码 | 公众号 |
|---|---|---|
| ![微信收款码](docs/site/media/收款码/微信收款码.jpg) | ![支付宝收款码](docs/site/media/收款码/支付宝收款码.jpg) | ![公众号二维码](docs/site/media/公众号/公众号二维码.jpg) |

> ⚠️ **联系方式与收款码只在宣传页上**（本文件会随源码交付，不写联系方式）。

## ❓ 常见问题

| 问题 | 回答 |
|---|---|
| **会自动下单吗？** | **不会**。不连接券商账户、不代下单、不经手资金，只做数据整理与研究结论输出 |
| 需要什么配置？ | **2 核 2 GB / 磁盘 ≥ 10 GB**，Ubuntu 22.04+（或自己的电脑）；实测最重作业峰值约 1.1 GB。⚠️ **系统时区必须是 `Asia/Shanghai`** —— 数据口径恒为上海时间，但定时任务按本机时区解释计划，时区不对会整体偏 8 小时且不报错（`xlp doctor` 会检查） |
| 数据源 Key 谁付？ | **自备自付**（同花顺 Fuyao API）。软件是一次性买断，数据源调用成本由使用者承担 |
| 需要会编程吗？ | 不需要。装好后每天自动跑，结果在网页看板或微信里看；会命令行更好排障 |
| 交付什么形式？ | 源码（私有仓库协作者 / 压缩包）+ 部署教程 + 全部文档；数据首次运行时自己拉（约 14 分钟） |
| 多久出结果？ | 竞价与选票 **09:25:30 前**；看天 09:27；盘中强多票每 30 分钟；复盘 16:08；明日观察 16:14 |
| 策略会不会失效？ | 市场会变，参数需要定期重标定；仓库带了重标定与自评脚本（`xlp stats` 现算真实战绩） |
| 能装云服务器吗？ | 能，2C2G 就够；也可以装在自己电脑上（常驻约 325 MB） |
| 与我自研 / 别家有什么区别？ | 见「PRO 版新增」：**真竞价数据 + 权威涨跌停口径 + 51 条质量闸 + 一套能自证的流程** |
| 支持退吗？ | 软件为**虚拟商品、源码可复制**，交付后不支持退款；建议先花 88 元试用 5~7 个交易日 |

## 📝 更新日志

> 格式：`### V<日期> (YYYY-MM-DD)` + `- 条目`（宣传页会从本文件解析渲染）。

### V20260923 (2026-09-23)
- **竞价扩成全市场**：每日每票一行，池票带来源标记；同日两批改成**一次取全市场**（池票缺失即不发产物），
  逐票判定定盘、没定盘的只重取那几只，仍没定盘就丢弃告警（**不写虚拟价**）
- **批量作业并发取数**：线程安全令牌桶 + 有界并发，56 个请求 **12.1s → 0.84s**（默认 12 QPS / 4 线程，可调回串行）
  * 覆盖：全市场竞价、股本校准、龙虎榜逐日、指数全量、标签成分股
- **盘中指标改为每轮从日初状态重算**：原来收盘前会收敛到当日价（MA5 偏 14%）
- **2C2G 达标复核**：`verify` 1.05 GB / `daily` 1.02 GB / 盘中冷启动 1.10 GB，余量约 0.5 GB
- **文档收敛**：21 份 md → 18 份（部署二合一、AI 工作流三合一），README 与宣传页同源有 CI 守卫

### V20260922 (2026-09-22)
- 早盘竞价与一进二**合成一条链**（09:25:20 起，竞价落库即选票，不再靠「差一分钟」保证顺序）
- 昨日连板票今日竞价加**板块**字段（`daily.json` 的 `multi_board`）
- 竞价一轮只记一个批次；「有行但没用」也算缺；加覆盖断言
- 指数指标补全历史后重跑 414 天自评，更新冻结数字

### V20260921 (2026-09-21)
- **涨跌停判定收成一张规则表** + 定点舍入；实时判定改原始价口径
- 指标表加**四维预测**两列（个股/指数 × 日线/盘中），算法只留一份
- 指数指标支持**批量补历史**（一次读 / 一次算 / 逐日替换），修「补历史被静默跳过」
- 盘中两张表改**单分区**：永久只有一份快照，跨天覆盖同一文件

### V20260920 (2026-09-20)
- 横扫**场景性 bug**：8 处同一病根 + 2 处数据损坏

### V20260919 (2026-09-19)
- 空数据湖**装机演练**：一次抓到 5 个只有干净环境才会暴露的问题
- 表结构变更的**旧分区迁移**通道（补列/改列 + 行数校验 + 原子替换）

### V20260917 (2026-09-17)
- 表数收敛到 **17 张**；数据字典改为**从 schema 生成**（CI 校验不许漂）
- 文档只留长期维护的活文档，一次性 review 结论并入活文档后删除

## ⚖️ 风险与许可

- 仅供**研究与学习**，**不构成任何投资建议**，**不承诺任何收益**；数据可能延迟、缺失或出错，据此操作风险自担。
- 本页**不展示胜率、收益、命中率等业绩数字**：那是营销话术的重灾区；真实战绩请用 `xlp stats` 在你自己的库上现算（自带幸存者偏差标注）。
- 不连接券商账户、不代下单、不经手资金；所有输出仅为研究结论。
- 软件为**专有许可**：可自用、可修改、可集成进自己的产品（含对外提供服务）；
  **禁止转售或再分发源码本身**；无担保，责任上限为已付价款。

> 完整条款见站点页脚：**用户协议 · 隐私政策 · 风险揭示书**；源码仓库内另有 `LICENSE` 与 `DISCLAIMER.md`。

---

<sub>完整文档（部署 / 运维 / 策略 / 数据字典 / 性能基线 / 编码规范）在源码仓库 `docs/`、`SKILL.md` 与 `CONVENTIONS.md`；对外宣传页 = `docs/site/index.html`（与本文件同源，CI 守卫）。</sub>
