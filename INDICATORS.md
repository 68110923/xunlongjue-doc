# 指标口径规范

> **唯一真源**。所有指标的公式、参数、单位以本文为准。
> 实现见 `src/xunlongjue_pro/calc/indicators.py`（**唯一实现**——旧项目有 4 处重复）。
> 设计依据：xunlongjue-pro-PLAN.md M3

---

## 0. 两条总原则

| # | 原则 |
|---|---|
| 1 | **参数向中国软件口径对齐**（通达信/同花顺），不照搬西方默认值。典型：RSI 用 6/12/24 而非 14；ATR 用简单均值而非 Wilder 平滑 |
| 2 | **缺失即 `null`** —— 数据不足周期时返回 `null`，**不用 0 或前值填充**（契约 §5） |

---

## 1. 均线类

### 1.1 简单移动平均 MA

$$\text{MA}(n)_t = \frac{1}{n}\sum_{i=0}^{n-1} C_{t-i}$$

| 周期 | 定位 |
|---|---|
| MA3 / MA5 / MA7 / MA10 / MA15 | 短线（打板主用） |
| MA20 / MA30 | 中短线（MA20 与 BOLL 中轨同源） |
| MA60 | 回撤闸基准 |

**不足 n 条时返回 `null`**（旧项目用「现有条数」求均值，会造成早期值失真 —— 新项目改为严格）。

### 1.2 均线多头排列（派生布尔量）

```
bullish_align = MA3 > MA5 > MA10 > MA20
```

比单看某条线信息量更高，成本近乎为零。任一 MA 为 `null` 则结果为 `null`。

---

## 2. 摆动指标

### 2.1 KDJ(9, 3, 3)

```
RSV_t = (C_t - L9_t) / (H9_t - L9_t) × 100        # L9/H9 = 近 9 日最低/最高
K_t   = 2/3 × K_{t-1} + 1/3 × RSV_t
D_t   = 2/3 × D_{t-1} + 1/3 × K_t
J_t   = 3K_t - 2D_t
```

- 初值：`K = D = 50`
- 分母为 0（9 日无波动）时 `RSV` 取前值；首日无前值时取 50

### 2.2 MACD(12, 26, 9)

```
DIF  = EMA(C, 12) - EMA(C, 26)
DEA  = EMA(DIF, 9)
BAR  = 2 × (DIF - DEA)
```

EMA 平滑系数 `α = 2/(n+1)`，首值取第一个收盘价。

> `BAR` 乘 2 是中国软件惯例（通达信 `MACD` 柱），西方常不乘。

### 2.3 RSI(6, 12, 24)

按**通达信 SMA** 口径（Wilder 平滑，M=1）：

```
U_t = max(C_t - C_{t-1}, 0)
D_t = |C_t - C_{t-1}|
SMA(X, n)_t = (X_t + (n-1) × SMA(X, n)_{t-1}) / n
RSI(n)_t = 100 × SMA(U, n)_t / SMA(D, n)_t
```

> ⚠️ 周期是 **6 / 12 / 24**，不是西方的 14 —— 这是中国软件默认值。

### 2.4 BIAS 乖离率(6, 12, 24)

$$\text{BIAS}(n)_t = \frac{C_t - \text{MA}(n)_t}{\text{MA}(n)_t} \times 100$$

衡量偏离均线程度。打板场景关心「高开多少算过高」。

---

## 3. 通道与波动

### 3.1 BOLL(20, 2)

```
MID   = MA(20)
STD   = 近 20 日收盘的**总体标准差**（除以 n，非 n-1）
UPPER = MID + 2 × STD
LOWER = MID - 2 × STD
WIDTH = (UPPER - LOWER) / MID
%B    = (C - LOWER) / (UPPER - LOWER)
```

> 标准差用**总体**口径（÷n），与通达信一致；用样本口径（÷n−1）会整体偏移。

### 3.2 ATR(14)

按中国软件口径用**简单均值**，非 Wilder 平滑：

```
TR_t = max(H_t - L_t, |H_t - C_{t-1}|, |L_t - C_{t-1}|)
ATR  = MA(TR, 14)
```

用途：**止损位计算**（比固定百分比科学）。

### 3.3 振幅

$$\text{振幅}_t = \frac{H_t - L_t}{C_{t-1}} \times 100$$

旧项目 `GATE_AMP_MAX = 13.6` 用的就是这个。

---

## 4. 量能类

| 指标 | 公式 | 说明 |
|---|---|---|
| **VMA5 / VMA10** | 近 5 / 10 日成交量简单均值 | 比「量比」更细的量能判断 |
| **量比** | `今量 ÷ 前 5 日均量`（**不含今日**） | 盘中口径 |
| **换手率** | `成交量 ÷ 流通股本 × 100` | ⭐ 新能力：流通股本 = `流通市值 ÷ 现价` |

> ⚠️ 量比的分母**不含今日** —— 含今日会导致「放量时量比被自己拉低」的失真。

---

## 5. 形态与位置类

| 指标 | 公式 |
|---|---|
| **N 日涨停次数** | 近 N 日内 `is_limit_up` 为真的天数（N = 4 / 10 / 20） |
| **距 60 日低点涨幅** | `(C - min(L, 60)) / min(L, 60) × 100` |
| **距 60 日高点跌幅** | `(C - max(H, 60)) / max(H, 60) × 100` |
| **60 日最大回撤** | `max over window (峰值 → 谷值) 跌幅`，**基于前复权价** |
| **缺口** | `(O_t - C_{t-1}) / C_{t-1} × 100` |
| **连板数** | **Fuyao 权威值**（`continue_day_cnt`），不再自行推断 |

> **回撤必须基于前复权价**：旧项目 `known-issues.md` 记录的「跨除权日失真」即因未复权。
> 新项目有完整复权因子事件流，从根上修掉。

### 5.1 复权口径（位置类指标的输入前提）

存储**只留原始价 + 除权事件**（`bronze/`），复权在视图层算：

| 视图 | 因子 | 锚点 |
|---|---|---|
| `v_daily_qfq` | `forward_factor(t) = C_last / C_t` | 窗口内**最新**交易日（末根还原为真实价） |
| `v_daily_hfq` | `backward_factor(t) = C_first / C_t` | 窗口内**最早**交易日（首根还原为真实价） |

其中 `C_t = ∏_{e ≤ t} scale`，单事件

```
scale = (前收 − 每股分红 + 配股价 × 配股比例) / (前收 × (1 + 每股送股 + 配股比例))
```

#### 事件必须按 `(thscode, ex_date)` 聚合，不能按主键去重

Fuyao 的 `adjustment-factors` dump 会把**同一次分配拆成多条记录**
（实测 57,305 行中有 3 组重复键）：

```
603883.SH  2024-06-27  dividend=0.16  bonus=0.00
603883.SH  2024-06-27  dividend=0.50  bonus=0.30
```

按主键去重只能留一条，等于丢掉一半分配方案。交易所除权参考价可裁决：

```
前收 26.56，除权日开盘 19.92
只取 0.16      → (26.56 − 0.16) / 1.30 = 20.31  ✗
只取 0.50/0.30 → (26.56 − 0.50) / 1.30 = 20.05  ✗
两行相加       → (26.56 − 0.66) / 1.30 = 19.92  ✓ 与开盘价一致
```

口径：**先 `DISTINCT` 去掉逐字段完全相同的真重复，再对分量求和**。
`allotment_price`（配股单价）是价格不是数量，取 `max` 而非求和。

> ⚠️ **锚点必须取窗口内的交易日，不能取「全部事件的总乘积」。**
> 事件表含已公告但**尚未除权**的未来除权日（可达今日之后数月）。
> 计入总乘积会造成前视偏差——回测中 2026-09 的价格用到了 2026-11 才发生的分红；
> 同时前复权末根 ≠ 真实成交价，实盘信号与回测口径不一致。
> 真实数据上 5,564 只中有 136 只曾命中此缺陷。

三条不变量由 `xlp calc check` 强制校验（违反即退出码 1）：

1. 前复权末根 == 真实成交价
2. 后复权首根 == 真实成交价
3. 因子恒为正

**指标该用哪个**：默认 `v_daily_qfq`。回撤、距高低点、均线一律用前复权。
后复权仅在需要「绝对值不随新事件变化」的长周期序列时使用。
区间涨跌幅用**原始价**会更接近看盘软件显示值（除非区间跨除权日）。

### 5.2 与旧项目的已归因差异（对拍基准见 §8）

对拍基准是旧项目的 `daily_kline` 列与 `hermes_stock_lib.py`。
完整考古见 `docs/OLD-PROJECT-INDICATORS.md`（含逐条文件:行号）。已确认的差异：

| # | 项目 | 旧项目 | 新项目 | 归因 |
|---|---|---|---|---|
| 1 | **60 日回撤的复权** | 号称用 pct 递推「伪复权链」，**实际未复权** | 基于 `v_daily_qfq`，真复权 | **旧项目缺陷**，见下 |
| 2 | **60 日回撤窗口** | 实际 61 根（`prev_trading_day(d,60)` 到 `d` 含两端） | 60 根 | 旧项目差一；新项目按「60 日」字面口径 |
| 3 | 窗口不足 | 数据 <10 根返回 `0.0`；10~59 根按实际条数算 | 一律 `null` | 遵循 §0 原则 2 |
| 4 | MA 不足周期 | **MA5 用现有条数求均值**（早期值失真）；MA10 才返回 `None` | 一律 `null` | 旧项目缺陷 |
| 5 | 数值精度 | `round(x, 1)` | 全精度 | 策略闸比较时需注意边界 |
| 6 | 涨停判定 | `pct >= 9.8` **全市场统一**，不分 ST / 创业板 / 科创板 / 北交所 | 按板块判定（10% / 20% / 30% / ST 5%），见 `calc/limit.py` | 旧项目缺陷 |
| 7 | 连板数 | 自算日期交叉（`pct > 9.5`） | **Fuyao 权威值** `continue_day_cnt` | 新项目更可靠 |

#### 差异 1 的实测影响（最重要）

旧项目 `_adj_chain()` 的 docstring 写「除权日被 pct 自动消化」，
但实测 `daily_kline.pct` **就是原始收盘涨跌幅**：

```
300910  2026-05-28  close 45.65
        2026-05-29  close 33.76   prev_close 45.65   pct -26.05
                     ↑ 除权日（每 10 股送 3 股派 1.5 元），pct 记录了原始跳空
```

反向递推 `P[i] = P[i+1] / (1 + pct[i+1]/100)` 于是把原始跳空**原样保留**，
得到的链是「原始价 × 常数」，`旧链/原始价` 在除权日两侧恒为 3.2999（实测）。
该股 2026-07-30 的真值 28.3%，旧项目给出 **46.7%**。

300 只股票 × 1176 组实测（窗口取 61 以排除差异 2 的干扰）：

| 指标 | 数值 |
|---|---|
| 与真值完全一致（≤0.05pp） | 79.8% |
| 旧值**偏高**（虚增回撤） | 174 组 |
| 旧值偏低（峰谷选择改变导致） | 64 组 |
| 偏差中位数 / p90 / 最大 | 0.20pp / 4.98pp / **34.10pp** |
| **48% 硬闸结论翻转** | 5 组（**0.43%**） |

方向是明确的：除权日跳空被当成真实下跌 → 回撤虚增 → **正常标的被误杀**。

> ⚠️ `GATE_MAX_DRAWDOWN = 48` 是在这批虚增值上调出来的。修正复权后
> 历史阈值不再等价，M4 必须重新标定（对拍时允许口径差异，但阈值要重跑）。


### 5.3 价格口径三分：原始 / 前复权 / 后复权

**指标该用哪个价格，是本节最容易被忽略的问题。** 三种口径的分工：

| 口径 | 视图 | 用途 | 禁忌 |
|---|---|---|---|
| **原始** | `v_kline_daily`（bronze 直读） | 涨停价 `round(前收×1.1, 2)`、股价区间、一手金额、成交量金额 | 跨除权日的涨跌幅/均线 |
| **前复权** | `v_daily_qfq` | **实盘与当日信号**（末根 == 真实成交价） | 长期回测（见下） |
| **后复权** | `v_daily_hfq` | **回测**（历史值永不改变，可复现） | 绝对价位判断 |

#### 关键性质：qfq 与 hfq 只差一个常数

```
qfq(t) / hfq(t) = C_last / C_first   ← 与 t 无关
```

实测 8 只股票、1000+ 交易日，极差 **1e-16**（浮点噪声）。推论：

- **比例型指标**（MA 比值、KDJ、MACD 交叉、RSI、BIAS、振幅、缺口、回撤、距高低）
  在 qfq 与 hfq 下**结果完全相同**，阈值可通用；
- **绝对价位型**（ATR 止损位、BOLL 上下轨、均线绝对价）差一个常数，
  阈值必须**绑定口径**，且应绑定 `hfq`（回测可复现）。

#### ⚠️ 前复权不能用于长期回测

每次新除权都会重算整个历史序列 → **回测结果事后改变、不可复现**。
回测一律用 hfq；只有实盘当日信号用 qfq（因为末根锚定真实价）。

#### ⚠️ 不要用接口自带的 `forward` / `backward`

`prices_historical` 提供 `adjust ∈ {none, forward, backward}`（默认 `forward`），
但实测其 `forward` **不是全局累计因子**，而是逐日用当天价格套

```
(P − 当日之后的现金分红) / (1 + 送转比例)
```

后果是**破坏收益率**。检验：「无除权区间内，复权收益率必须等于原始收益率」：

| 股票 | 接口 `forward` 最大失真 | 本项目 `v_daily_qfq` |
|---|---|---|
| 600519.SH | 1.87 pp | **0.0000 pp** |
| 000001.SZ | 2.77 pp | **0.0000 pp** |
| 603883.SH | 4.19 pp | **0.0000 pp** |
| 300910.SZ | 2.36 pp | **0.0000 pp** |
| 601998.SH | 4.51 pp | **0.0000 pp** |
| 688059.SH | 2.47 pp | **0.0000 pp** |

非保收益的序列会让 MA / BOLL / MACD / 回撤**全部失真**，故**不可用于指标计算**。
本项目只把它当**交叉校验基准**（接口 `none` 与本地原始价逐值一致，最大差 0.000000）。

> 接口 `start` / `end` 是**毫秒时间戳**。传 `YYYYMMDD` 不报错但返回 **0 行**。
> 该陷阱已由 `Endpoint.ms_params` 本地拦截。

### 5.4 基础派生字段（`calc/derivations.py`）

这些字段是「价格本身的属性」，**与复权口径无关** —— 全部定义在
**原始价 + 除权参考价**之上。单独一层，避免在指标层重复实现一遍除权参考价
与涨停判定（此前两处各写了一份）。

| 字段 | 公式 | 说明 |
|---|---|---|
| `prev_close` | 前一交易日**原始**收盘 | |
| `reference_price` | `(前收 − 分红 + 配股价×配股比) / (1 + 送股 + 配股比)` | **除权参考价**，所有比值的基准 |
| `pct` | `(C − 参考价) / 参考价 × 100` | **官方口径**，除权日不含跳空 |
| `pct_raw` | `(C − 原始前收) / 原始前收 × 100` | 原始口径，**等于旧项目 `pct`** |
| `amplitude` | `(H − L) / 参考价 × 100` | 振幅 |
| `gap` | `(O − 参考价) / 参考价 × 100` | 缺口 |
| `limit_up_price` | `round(参考价 × (1+比例), 2)` | 四舍五入到分 |
| `limit_down_price` | `round(参考价 × (1−比例), 2)` | 同上 |
| `is_limit_up` / `is_limit_down` | 收盘触及对应价 | 分板块，见 `calc/limit.py` |
| `limit_ratio` | 主板 0.10 / 创业板·科创板 0.20 / 北交所 0.30 / 主板 ST 0.05 | |

**除权参考价不重复实现 `scale`**，而是从已校验的复权视图反推：

```
reference(t) = qfq(t−1) × raw(t) / qfq(t)
```

> ⚠️ 该式要求传入**复权视图**。若传 `v_kline_daily`，两套价格相同、式子退化成
> `prev_close`，除权日的参考价/涨跌幅/涨跌停价**全部错成原始口径** ——
> 实测 603883 除权日会被误判为「跌停」。代码对 `price_view="v_kline_daily"`
> **显式报错**。

**独立印证**：603883 于 2024-06-27 除权（10 送 3 派 6.6），算得参考价
**19.9231**，而当日开盘 **19.92** —— 缺口 `−0.015%`。交易所的除权参考价
与本地推导吻合到分，这是比「与旧项目对拍」更强的证据。

**命名易混**：

| 列名 | 含义 | 单位 |
|---|---|---|
| `turnover` | **成交额**（沿用 Fuyao 原生字段名） | 元 |
| `turnover_rate` | **换手率**（在 `indicators.py`，需流通股本） | % |

---

## 6. 全量清单

| # | 指标 | 类型 | 参数 |
|---|---|---|---|
| 1 | MA3 / MA5 / MA7 / MA10 / MA15 / MA20 / MA30 / MA60 | 窗口 | 8 条 |
| 2 | 均线多头排列 | 派生 | MA3>MA5>MA10>MA20 |
| 3 | KDJ | 递归 | (9,3,3) |
| 4 | MACD | 递归 | (12,26,9)，BAR×2 |
| 5 | RSI | 递归 | (6,12,24) |
| 6 | BIAS | 窗口 | (6,12,24) |
| 7 | BOLL | 窗口 | (20,2)，含 width / %B |
| 8 | ATR | 窗口 | (14)，简单均值 |
| 9 | 振幅 | 派生 | — |
| 10 | VMA5 / VMA10 | 窗口 | 2 条 |
| 11 | 量比 | 派生 | 分母不含今日 |
| 12 | 换手率 | 派生 | 需流通市值 |
| 13 | N 日涨停次数 | 窗口 | N=4/10/20 |
| 14 | 距 60 日高低点 | 窗口 | — |
| 15 | 60 日最大回撤 | 窗口 | 基于前复权价 |
| 16 | 缺口 | 派生 | — |
| 17 | 连板数 | **Fuyao** | 权威值 |

**已排除**（并记录理由，避免以后反复讨论）：

| 指标 | 排除理由 |
|---|---|
| MA120 / MA250 | 对短线打板无指导意义 |
| OBV | 日线上噪声大，需分钟级才稳定 |
| CCI / WR | 与 KDJ/RSI 高度共线，只增加调参空间 |
| DMI / ADX | 趋势强度，与打板的日内/隔日尺度不匹配 |
| TRIX / ROC | 偏中长线 |
| 筹码分布 | 数据源不支持，需自建模型 |

> **共线性提醒**：CCI/WR/RSI/KDJ 本质都在测「超买超卖」。指标越多，回测过拟合风险越大 ——
> 旧项目只用 4 个指标就产生了有效策略，新增指标必须**有明确用途**才加。

---

## 7. 实现分工

| 类型 | 实现方式 | 理由 |
|---|---|---|
| **窗口类**（MA / BOLL / BIAS / ATR / VMA / 回撤 / 距高低） | **DuckDB 窗口函数** | 列式一次扫描；1000 万行秒级 |
| **递归类**（KDJ / MACD / RSI） | **pandas `groupby().ewm()`** | 递推不适合 SQL 窗口函数 |

**是否物化落盘**：暂定**不落盘**（DuckDB 按需算足够快，落盘会多一份一致性负担）。
若盘中全市场高频重算成为瓶颈，再物化到 `calc/indicator_daily`。

### 7.1 内存必须受控（实测踩过的坑）

**一次性把「股票 × 全历史」交给 pandas 会 OOM。** 实测全市场单日请求：

```
Out of memory: Killed process (python)  anon-rss:13843560kB   ← 13.8 GB
机器总内存 15 GB
```

原因不是数据本身大，而是**放大倍数**：1,000 万行 × 48 列进 pandas 后，
`ewm` 的哨兵行 `concat` 又复制一份，KDJ 还调两次 —— 峰值约为数据的 6~8 倍。
**盘中每 30 分钟跑一次必然复发**，且不能靠加内存解决（数据湖还在长）。

三条硬约束（`compute()` 的参数，全部有回归测试）：

| 约束 | 默认 | 作用 |
|---|---|---|
| `chunk_size` | 1500 | 按股票分块，峰值内存与**总股票数无关** |
| `warmup_bars` | 250 | 每股只回看 N 根，用**日期边界**裁剪分区 |
| `start` 锚定 | — | 预热只砍 `start` **之前**的数据，**绝不截断请求区间** |

`warmup_bars` 的收敛性：递归类指标初值影响按指数衰减 ——
MACD 慢线 EMA26 → `0.926^n`，n=250 时约 `4e-9`；KDJ α=1/3 → `1e-44`。
未指定 `start` 时会按预热截断历史并**打 WARNING**（静默截断比报错危险）。

### 7.2 性能优化记录

| 改动 | 单日全市场耗时 | 峰值内存 |
|---|---|---|
| 初版（不分块、全历史、`transform(lambda)`） | **OOM 被杀** | 13.8 G |
| 分块 1500 + `row_number` 预热 | 94.2 s | 1.98 G |
| 改用**日期边界**替代 `row_number` | 77.6 s | 1.02 G |
| `groupby().ewm()` 替代 `transform(lambda s: …)` | 48.0 s | 1.00 G |
| 回撤直接索引 numpy（不再每股票构造 DataFrame） | **34.7 s** | **1.02 G** |
| 抽出 `derived_ctes` 复用（多一次 JOIN，但消除重复实现） | 45.1 s | 1.08 G |

两条最有价值的改动：

1. **`groupby().ewm()` 比 `transform(lambda s: s.ewm(...))` 快 6.6 倍**
   （1500 组、37.5 万行：0.89s → 0.13s），且结果逐值相同。前者走 pandas 的
   Cython 路径，后者每个分组回调一次 Python。
2. **回撤步骤不要 `frame.iloc[positions]`** —— 那会为每只股票构造一个
   `250 行 × 48 列` 的 DataFrame。直接索引 numpy 数组即可。

> 这两条都属于「写法问题」而非算法问题：算法没变，结果逐值不变
> （`test_chunk_size_does_not_change_results` 断言分块完全透明）。

---

## 8. 验收方式

**与旧项目逐值对拍**（`scripts/parity_indicators.py`）：

- 抽样若干股票 × 若干日期
- 比对 MA5/MA10、KDJ、MACD、BOLL、量比、回撤
- 允许误差 `1e-6`（浮点）
- 差异必须逐条归因（是参数口径变了？还是实现错了？）

**这个窗口只有一次** —— 等旧项目退役就无从对齐。

### 8.1 实测结果（2026-09-16）

旧库 `daily_kline` 已物化 `ma5/ma10/kdj_*/macd_*/boll_*/volume_ratio`，可直接对拍。
60 只股票 × 2025-01 至今，约 2 万个点：

| 指标 | 中位差 | p99 | 归因 |
|---|---|---|---|
| `ma5` | 0.002 | 0.079 | 数据源差异 |
| `ma10` | 0.003 | 0.005 | 数据源差异 |
| `kdj_k/d/j` | 0.003 | 5.2 / 10.2 / 7.6 | 复权口径 + 盘中污染 |
| `macd_dif/dea/bar` | 0.0001 | 0.66 / 0.55 / 0.29 | 复权口径（绝对值随比例缩放） |
| `volume_ratio` | 0.0025 | 0.005 | 数据源差异 |
| `boll_width` | **0.00003** | 0.0008 | ✓ |

**限定在「前 60 日无除权事件」的窗口内**（排除复权口径干扰）后，尾部大幅收敛：

| 指标 | 中位差 | p99 |
|---|---|---|
| `ma5` | 0.002 | 0.004 |
| `kdj_k` | 0.0026 | 0.163 |
| `boll_width` | 0.000026 | 0.00075 |
| `macd_dif` | 0.000085 | 0.055 |

剩余差异的三条归因（均已定位，非实现错误）：

1. **复权口径**：旧库价格本身是复权价（非原始价），且其回撤链用原始涨跌幅 —— 见 §5.2。
2. **数据源差异**：同为「原始收盘价」，16,476 个对拍点中有 **58 点不同**（0.35%）。
3. **旧项目盘中污染**：其 MA/KDJ/MACD/BOLL 路径**都不过滤 `is_intraday`**
   （只有 `volume_ma5` 过滤），故被 198 条盘中行污染。本项目统一用日线。

### 8.2 不变量（比「与旧项目对拍」更强的验收）

对拍只能证明「和旧实现一致」，**不能证明旧实现是对的**。故补充**独立实现**对拍：

| 检验 | 方法 | 结果 |
|---|---|---|
| 滑窗回撤 | 与逐行 O(n·W) 朴素双循环比对 | 214,588 点，最大差 **0.00e+00** |
| 复权保收益 | 无除权区间内复权收益率 == 原始收益率 | 失真 **0.0000 pp**（接口 `forward` 为 1.6~4.5 pp） |
| 复权连续性 | 400 个送股事件除权日涨跌幅 | 原始价 −28.83% → 前复权 **−0.00%** |
| 口径一致性 | `boll_mid` 与同周期 `ma` | 逐值相等 |
| 比例型指标 | qfq 与 hfq 输出 | 逐值相等（rtol 1e-12） |

> **教训**：`boll_width` 曾因生成 SQL 时字符串拼接的**运算符优先级**恒为 `0`
> —— `(A + B) - (A - B)` 被展开成 `A + B - A - B`。而 `boll_upper` /
> `boll_lower` 单独看完全正常，只有对拍才能发现。此类缺陷已加回归测试。
